apiVersion: v1
kind: Service
metadata:
  name: hello-nginx
  namespace: hello-nginx
  labels:
    app: hello-nginx
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: hello-nginx
